# Critique Report: 主助理对话透明与可控（含 tasks.md）

**Date**: 2026-06-03（第二轮，tasks 后）
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Reviewed**: spec.md, plan.md, research.md, data-model.md, contracts/, tasks.md, constitution.md, 前序 critique-20260603.md
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Executive Summary

本轮在前序 critique（已闭环 E1–E5/X2/P3/P4）之后进行，且 **tasks.md 已生成**，重点转向两件事：① 核对前序修订是否在任务里有真实落点；② 以"带任务"的新视角找剩余盲点。

**可追溯性结论：除 E3 外全部有任务落点。** E1→T008/T003、E2→T003/T009、E4→T032/T028/T029、E5→T030/T033/T045、X2→T054/T056、P3→T012/T019、P4→T012/T022 均可对应。**唯一断链是 E3**——前序已把"压缩历史回看以规整概要展示"折入 spec FR-020/SC-004 并要求补行为测试，但 tasks.md 既无"压缩回合 → 规整概要"的渲染任务，也无对应测试，存在**需求无任务**的缺口。

新审另发现 4 项 💡 与 2 项 🤔，其中最有价值的是 **X1：用户在高危确认 pending 时点"停止"** 的交互——AgentLoop 此刻阻塞在确认等待里，够不到取消检查点，停止会"看起来没生效"，且牵涉必须保留的 CC-001 同步确认协议。其余为历史保真度（reasoning 步骤）、transcript REST 脱敏对称性、早停竞态。整体 spec/plan 依旧扎实，无架构级返工，建议把下列低成本项折进 tasks/spec 后再进实现。

---

## Product Lens Findings 🎯

### Problem Validation
问题真实、用户亲述，前序已确认。无新发现。

### User Value Assessment
P1 切片（门控+停止）作 MVP、实时全量时间线本期就要——前序已确认（P2/X1）。无新发现。

### Alternative Approaches
"继续任务=唤醒主助理复用 `continue_subagent`" 仍是最优简化。无新发现。

### Edge Cases & UX
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | 🤔 | **回合以 `failed` 结束时仍自动派发排队消息**（data-model §1 表 + FR-009：离开 running 即派发，含 failed）。但助理刚失败就自动把下一句发出去，可能叠加失败或令用户困惑；这与"以反问/成功结束自动派发"的直觉不同。 | 拍板：failed 结束时是否照常自动派发；若否，failed 视同 `cancelled` 退回草稿（更安全），并据此修订 data-model §1 与 FR-009。 |
| P2 | 💡 | **双击是卡片详情与排队编辑的主交互**，无键盘可达替代、也缺可见提示（FR-011 双击进编辑、FR-023 双击展开详情）。可发现性与可访问性偏弱。 | 卡片/排队框加可见"双击查看/编辑"提示或显式按钮，并提供键盘可达入口（Enter/Space 打开详情、明确的"编辑"动作），不把双击作为唯一入口。 |

### Success Measurement
SC-001~SC-008 多数可测（P4 已收口）。无阻塞性新发现（事件上限/合并无对应可观测指标，单用户本地可接受）。

---

## Engineering Lens Findings 🔬

### Architecture Soundness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E4 | 💡 | **实时时间线与历史重建的步骤保真可能不一致**。R3 仅对"带 tool_calls 的中间消息"与"tool 结果"emit；data-model/contracts 又把 `kind=reasoning` 列为独立取值。若存在不随 tool_call 落库的纯 `reasoning` 步骤被实时 emit，则 transcript 从 `MessageRepository` 重建时无法还原 → 实时看到、历史看不到。 | 明确：`kind=reasoning` 仅作为随 tool_call 中间消息落库的伴随文本（可重建），不单独 emit 不落库的纯推理；或显式接受并文档化该 divergence。在 T034/T030 的重建口径里固化此约定并加一条对齐测试。 |

### Failure Mode Analysis
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E3 | 💡 | **早停竞态**：用户发消息后立即点停止，若停止到达时 worker 线程尚未 `run_context.begin()` 登记 Event，`cancel_session` 找不到 Event 返回 `accepted=false`，但该回合随后照常启动且**不被取消**。E2 的代际 token 解决的是"陈旧 set 误杀新运行"，未覆盖此反向窗口。 | `begin()` 时检查一张"待停止 sessionId"集合（停止先登记意图），命中则立即按取消起步；或停止端点对"消息已受理但运行未起"返回 pending-cancel 而非 false。补竞态回归测试。 |

### Security & Privacy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E2 | 💡 | **transcript / subagents REST 重建可能只截断、未走脱敏**。E5 已让实时 `assistant.activity/subagent` 走 009 payload allowlist，但 http-endpoints.md 对 `GET …/transcript`、`GET …/subagents` 仅写"文本截断、不暴露内部术语"，未明确同样脱敏。历史读路径是与事件流并列的数据出口，留只截断不脱敏会形成不对称泄漏面（工具入参/结果）。 | 让 T034/T035（transcript）与 T046/T047（subagents）读模型显式复用与实时同一套 009 脱敏，而非仅截断；contracts/http-endpoints.md 补此约束；T030/T042 补"REST 重建同样脱敏"断言。 |

### Testing Strategy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | 🎯 | **FR-020 压缩历史分支无任务、无测试**。前序 E3 已定"被压缩回合以规整概要展示（复用'之前的对话内容'折叠样式）、不报错、不补持久化"，并要求补行为测试。但 tasks.md 的历史重建任务（T034/T037/T039/T031）只覆盖"详细步骤仍在"的常态，未覆盖"已压缩→规整概要"渲染，也无对应测试 → 需求未落任务。 | 新增：①后端在 transcript 重建口径里标记"该回合已压缩/步骤不全"（不补持久化）；②前端 `ActivityTimeline` 对该标记渲染规整概要（复用既有折叠样式）；③前端单测覆盖"压缩回合展开=规整概要、不报错"。可挂到 US3 阶段（T034/T038/T031 附近）。 |

### Operational Readiness
单用户桌面、无迁移、回滚=revert。无新发现。

### Dependencies & Integration
依赖 013 既有 `continue_subagent` / `_resolve_subagent_session`，T056 已正确复用。无新发现。

---

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|----------------|--------------------|------------|
| X1 | **高危确认 pending 时点"停止"**：AgentLoop 阻塞在既有确认 `threading.Event` 等待中，停止只 set 取消 Event，但循环此刻不在任何取消检查点 → 停止在用户处理完确认前"看起来没生效"。这是 FR-006（随时可停）与 CC-001（保留同步确认协议）的交汇盲点，spec 边界与 tasks 均未覆盖。 | 用户在等确认时点停止却无反应，违背"随时能停"的核心信任承诺。 | 取消信号与确认等待是两条独立 `threading.Event`，需定义二者交互，且不得破坏 004/CC-001 的 first-decision-wins 与 `expires_at` 语义。 | 拍板停止语义：点停止是否同时让 pending 确认 fail-closed 当拒绝并唤醒循环到取消检查点。补一条"确认 pending 时停止"的集成/门卫测试，断言 CC-001 协议不回归。 |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 1 |
| 💡 Recommendations | 4 |
| 🤔 Questions | 2 |
| Product findings | 2 |
| Engineering findings | 4 |
| Cross-lens findings | 1 |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| E1 | Engineering | 🎯 | Testing/Requirement-Coverage | FR-020 压缩历史"规整概要"分支无任务无测试 | 加后端压缩标记 + 前端规整概要渲染 + 单测，挂 US3 |
| X1 | Both | 🤔 | UX × Reliability | 高危确认 pending 时点停止够不到取消检查点、看似无效 | 定义停止与 pending 确认交互（fail-closed 拒绝并唤醒）+ 测试，守 CC-001 |
| P1 | Product | 🤔 | Edge/UX | failed 结束仍自动派发排队消息或非所愿 | 拍板 failed 是否退回草稿；据此修订 data-model/FR-009 |
| E2 | Engineering | 💡 | Security | transcript/subagents REST 重建可能只截断未脱敏 | REST 读模型复用 009 脱敏 + contracts 补约束 + 断言 |
| E3 | Engineering | 💡 | Failure | 早停竞态：停止早于 run_context.begin 登记 → 不被取消 | begin() 查待停止集合 / 停止返回 pending-cancel + 回归测试 |
| E4 | Engineering | 💡 | Architecture | 纯 reasoning 步骤实时可见但历史不可重建 | 固定 reasoning 仅随 tool_call 落库口径或文档化 divergence + 对齐测试 |
| P2 | Product | 💡 | Edge/UX | 双击为唯一主交互、缺键盘可达与提示 | 加可见提示 + 键盘可达入口 |

---

## Recommended Actions

### 🎯 Must-Address（进实现前）
1. **E1**：在 tasks.md US3 阶段补 FR-020 压缩历史落点——后端 transcript 重建标记"已压缩/步骤不全"（不补持久化），前端 `ActivityTimeline` 渲染规整概要（复用既有折叠样式），并补前端单测；可在 T034/T038/T031 附近新增 2–3 个任务。

### 💡 Recommendations（强烈建议）
1. **E2**：tasks.md T034/T035/T046/T047 显式复用 009 payload allowlist 脱敏；contracts/http-endpoints.md 把"走脱敏"写进约束；T030/T042 补断言。
2. **E3**：在 T003/T004/T015 范围内补"早停竞态"处理（待停止集合或 pending-cancel 返回）与 T009 同档的回归测试。
3. **E4**：在 research/data-model 固定 `kind=reasoning` 的落库口径，T028/T034 加一条"实时与历史步骤对齐"测试，或显式文档化可接受的 divergence。
4. **P2**：spec/plan 的 UX 描述与前端任务（T038/T050、T025）补"双击有可见提示 + 键盘可达替代"。

### 🤔 Questions（需拍板）
1. **X1**：用户在高危确认 pending 时点"停止"，是否同时让该确认 fail-closed 当拒绝并唤醒循环？（需保留 CC-001）
2. **P1**：回合以 `failed` 结束时，排队消息是照常自动派发，还是退回普通草稿（更安全）？

---

**Severity Legend**：🎯 阻塞实现 ｜ 💡 强烈建议 ｜ 🤔 需拍板

---

## Resolution（已应用 2026-06-03）

用户拍板（X1=停止把待确认当拒绝；P1=failed 退回草稿）+ 全部折入：

- **E1（🎯→已落）**：tasks 新增 T034a（后端 transcript 重建标记"已压缩/步骤不全"，不补持久化）+ T038a（前端 `ActivityTimeline` 渲染规整概要）；扩展 T031 测试覆盖"压缩回合展开=规整概要、不报错"；contracts/http-endpoints.md 补"已压缩标志"。FR-020 现有完整任务落点。
- **X1（🤔→已定）**：spec 新增 FR-006b + 边界——停止时 pending 高危确认 fail-closed 当拒绝并唤醒到取消检查点，保留 CC-001；tasks T015 补实现、新增 T011a 集成测试。
- **P1（🤔→已定）**：failed 结束排队消息退回草稿。已改 spec FR-009/FR-014 + 边界、data-model §1 failed 行（自动派发→否）、tasks T023/T024（仅 succeeded/waiting_for_user 自动派发）。
- **E2 💡**：transcript/subagents REST 重建显式走 009 payload allowlist 脱敏。已改 contracts/http-endpoints.md §2/§3、tasks T030/T034/T042/T046。
- **E3 💡**：早停竞态——run_context 维护"待停止集合"，`begin()` 命中即取消。已改 tasks T003/T004，回归测试并入 T009。
- **E4 💡**：固定 `kind=reasoning` 仅随 tool_call 中间消息落库（实时=历史口径）。已改 data-model §3、tasks T028/T034。
- **P2 💡**：双击非唯一入口——加可见提示 + 键盘可达。已改 spec FR-011/FR-023、tasks T025/T050。

**复评建议**：可选 `/speckit-analyze` 做一次 spec↔tasks 一致性核对，或直接进 `/speckit-implement`。

---

*Generated by `/speckit.critique` — 双视角战略+技术评审（第二轮，tasks 后）。*
