# Critique Report: Agent Built-in Tools Upgrade

**Feature**: `015-agent-builtin-tools-upgrade`
**Generated**: 2026-06-08 12:50:49
**Inputs Reviewed**: `spec.md`, `plan.md`, `research.md`, `data-model.md`, `contracts/`, `quickstart.md`, `.specify/memory/constitution.md`
**Tasks Reviewed**: None; `tasks.md` has not been generated.
**Previous Critiques**: `critique-20260608-123633.md`
**Template Note**: `.specify/templates/critique-template.md` was not present in this worktree, so this report follows the required critique structure directly.

## Executive Summary

The previous must-address gaps have been resolved. The updated spec and plan now include:

- explicit observed failure modes and MVP delivery slices
- a configuration contract with keys, defaults, validation, getter names, docs/template obligations, and UI exposure decisions
- a raw-output artifact security model covering storage, repository boundary, creation failure fallback, retention, authorization, sensitive-data handling, and leak tests
- a baseline policy requiring current baselines for existing-file replace/edit/delete/update while allowing baseline omission only for new-file creation
- AgentLoop result-governance save boundary coverage, process failure modes, migration/cleanup/recovery, runtime health metadata, and stronger verification paths

The feature is now ready to proceed to task generation. Remaining findings are recommendations to preserve the quality of task decomposition and implementation, not blockers.

**Verdict**: ✅ **PROCEED**. Run `/speckit-tasks` to break the plan into actionable tasks.

## Product Lens Findings

### 3a. Problem Validation

The problem is clear and grounded in concrete observed failure modes. The new evidence section explains stale writes, oversized outputs, inconsistent contracts, long-running command ambiguity, and fragmented permission handling.

No blocking product issue found.

### 3b. User Value Assessment

The MVP is now explicit and sequenced into four slices. Slice 1 targets the highest-value and lowest-risk capability: structured results, workspace policy, safe reads, baseline-protected mutation, and AgentLoop pairing tests.

**P1 - 💡 Keep task generation slice-oriented**

The plan is broad by design. If `/speckit-tasks` creates a flat task list, implementation may still drift into cross-slice coupling.

Suggestion: Generate tasks grouped by Slice 1 through Slice 4, with each slice ending in contract, integration, and guardrail tests.

### 3c. Alternative Approaches

The search approach is now flexible: native traversal or `rg --json` can sit behind the same structured contract if large-repo fixtures exceed caps. This is a good balance between product performance and implementation portability.

No blocking product issue found.

### 3d. Edge Cases & User Experience

Outside-workspace reads are now framed as exceptional access, and confirmation summaries have concrete safe examples. Agent continuation is now represented in SC-010 and quickstart coverage.

**P2 - 💡 Preserve confirmation copy in tasks**

Confirmation wording is security-sensitive and product-sensitive. The current examples are good, but they need to become implementation tasks and tests rather than remaining prose.

Suggestion: Add explicit tasks for confirmation summary builders and leak tests for outside-workspace read, file mutation, and elevated command execution.

### 3e. Success Measurement

Success criteria now include runtime health diagnostics without leaking secrets or local paths. This closes the prior measurement gap for operational health.

No blocking product issue found.

## Engineering Lens Findings

### 4a. Architecture Soundness

The architecture respects project layering: business/tool contract logic, execution/process control, data/repository metadata, and no UI/desktop API execution-state ownership. The result-governance boundary is now explicit enough for tasks.

**E1 - 💡 Pin the workspace runtime carrier during tasks**

The plan says authorized workspace is resolved from Agent runtime context so subagents and specialists inherit it. That is directionally sound, but the exact code carrier still needs to be chosen during tasks.

Suggestion: Add an early Slice 1 task to define and test the workspace runtime context API, including assistant, ephemeral subagent, and specialist inheritance.

### 4b. Failure Mode Analysis

Process lifecycle and artifact failure modes are substantially improved. The plan now covers stream pumping, log caps, duplicate detection, termination, stdin, shutdown cleanup, and artifact write fallback.

No blocking engineering issue found.

### 4c. Security & Privacy Review

The raw artifact security model is now concrete. It defines internal storage, repository-only metadata access, owner-session/workspace authorization, retention behavior, metadata safety, encryption deferral, and leak tests.

**E2 - 💡 Treat encryption deferral as an explicit acceptance note**

Deferring encryption is reasonable for this local-only desktop feature, but it should not disappear during implementation.

Suggestion: Add a task to document encryption deferral in `docs/PROJECT_CONSTRAINTS.md` and verify private-permission best effort where supported.

### 4d. Performance & Scalability

Traversal caps and search backend flexibility are now specified. The plan gives enough direction for bounded behavior and optional optimized backend use.

No blocking engineering issue found.

### 4e. Testing Strategy

The test strategy is strong: unit, integration, data, process, guardrail, schema/prompt, leak, and large-fixture tests are all represented.

**E3 - 💡 Make schema validation executable**

The common JSON schema and markdown contract are useful, but task generation should turn them into executable checks.

Suggestion: Add tasks for schema validation helpers and tests that validate representative envelopes for each tool family against `tool-result-envelope.schema.json` plus per-tool payload validators.

### 4f. Operational Readiness

Migration, cleanup, recovery, and runtime health diagnostics are now described well enough for task generation. Forward-only migration handling is explicit.

No blocking engineering issue found.

### 4g. Dependencies & Integration Risks

No new third-party dependency is required by the plan. Optional `rg --json` use is correctly isolated behind the search contract rather than making shell output part of the public API.

No blocking engineering issue found.

## Cross-Lens Insights

**X1 - 💡 Slice gates are the main remaining risk-control mechanism**

Both product value and engineering safety depend on not mixing all tool families into one delivery. Slice gates should require a passing contract and guardrail suite before moving to the next slice.

Suggestion: In `/speckit-tasks`, structure dependencies so Slice 2 depends on Slice 1 contract/guard tests, Slice 3 depends on core governance, and Slice 4 depends on repository/migration foundations.

**X2 - 💡 Documentation tasks are not optional cleanup**

The constitution requires active docs to match current behavior. Since this feature introduces new tool boundaries, configuration, and raw artifact retention, docs updates should be first-class tasks.

Suggestion: Include explicit tasks for `docs/PROJECT_CONSTRAINTS.md`, `config.example.json`, and any affected Agent guidance, not just implementation code.

## Findings Summary Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| P1 | Product | 💡 | User Value | Broad plan still needs slice-oriented task generation | Group `/speckit-tasks` output by Slice 1-4 with acceptance gates |
| P2 | Product | 💡 | UX | Confirmation copy must become tested implementation, not just prose | Add summary-builder tasks and leak tests for each high-risk confirmation |
| E1 | Engineering | 💡 | Architecture | Workspace runtime carrier is directionally defined but not pinned to a code API | Add early task for workspace runtime context API and inheritance tests |
| E2 | Engineering | 💡 | Security | Encryption deferral is acceptable but needs explicit implementation/documentation handling | Add docs and best-effort permission tasks |
| E3 | Engineering | 💡 | Testing | Contract schema needs executable validation coverage | Add schema validation helpers and per-tool payload validator tests |
| X1 | Both | 💡 | Scope x Risk | Slice gates are the key control against over-broad implementation | Encode slice dependencies and gates in `tasks.md` |
| X2 | Both | 💡 | Docs x Governance | Active docs/config templates are part of the constitutional delivery surface | Add first-class docs/config tasks |

## Previous Must-Address Closure

| Previous ID | Status | Evidence |
|-------------|--------|----------|
| E1 Config surface | Closed | `plan.md` now has `Configuration Contract` with keys, defaults, validation, getters, docs/template obligations, and UI exposure. |
| E5 Raw-output threat model | Closed | `plan.md` has `Raw Output Artifact Security Model`; `data-model.md` adds artifact write and authorization entities. |
| X1 Baseline policy conflict | Closed | `spec.md`, `data-model.md`, `contracts/foundational-tools.md`, `research.md`, and `quickstart.md` now require baselines for existing-file mutation and allow omission only for new file creation. |

## Suggested Remediation Edits

No must-address edits are required before `/speckit-tasks`.

Recommended task-generation emphasis:

1. Group tasks by Slice 1-4 with explicit acceptance gates.
2. Add early tasks for workspace runtime context API and inheritance tests.
3. Add schema/payload validator tasks tied to `contracts/`.
4. Add docs/config template tasks for unified config and raw artifact security.
5. Add confirmation summary builders and leak tests.

## Verdict

✅ **PROCEED**

The spec and plan are solid enough for task generation. No constitution violation or implementation-blocking product/engineering gap remains.

## Post-Critique Actions

Run `/speckit-tasks` to break the plan into actionable tasks. When generating tasks, carry forward the non-blocking recommendations above as task-shaping constraints.
